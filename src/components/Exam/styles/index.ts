/* eslint-disable import/prefer-default-export */
import { colors } from "../../../styles/colors";

export const styleBlock = {
  boxShadow: `${colors.bgBlue} 0px 4px 24px`,
  border: `solid 1px ${colors.blue}`
}